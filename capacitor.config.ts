import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId: 'io.snacke.live', appName: 'Snacke LIVE', webDir: 'dist', server: { androidScheme: 'https' } };
export default config;
